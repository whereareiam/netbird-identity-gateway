# Keep the upstream server binary; add the database client for startup recovery.
FROM netbirdio/netbird-server:0.77.1
RUN apt-get update \
    && apt-get install -y --no-install-recommends postgresql-client \
    && rm -rf /var/lib/apt/lists/*
